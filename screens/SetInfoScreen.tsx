import React from "react";
import SetInfoForm from "../components/SetInfo/SetInfoForm";

type Props = {};

const SetInfoScreen = (props: Props) => {
  return <SetInfoForm {...props} />;
};

export default SetInfoScreen;
