import { StyleSheet } from "react-native";

const setInfoStyles = StyleSheet.create({
  containerSetInfo: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },

  headerText: {
        fontSize: 20,
  },
  datePickerStyle: {
    width: 200,
    marginTop: 20,
  },

});

export default setInfoStyles;
